schema: learn/baseline-probes
kind: 摸底
goal: ai-agent-engineer
note: questions only. No expect in the HTML. Skip any language. Stop if sore. Not today's DAY LOAD. Four expression boxes: en zh fr de. Speak in Chrome or Edge writes into that box. en C1 and de B2+ are attested SPRACHEN. zh and fr are samples the human asked for, not SPRACHEN lines. Do not add Chinese as native.
probe:
  id: AgentCore
  q: In your own words: what is Amazon Bedrock AgentCore, and what is it not?
probe:
  id: AgentToolkit
  q: In your own words: what is Agent Toolkit for AWS, and what is it not?
probe:
  id: LambdaMicroVM
  q: In your own words: what is a Lambda MicroVM doing for a coding agent?
probe:
  id: AgentLoop
  q: What is an agent loop, and how does it decide when to stop?
probe:
  id: MCP
  q: What is Model Context Protocol, and what problem does it solve for tools?
probe:
  id: AgenticEngineering
  q: If an agent generates the decision logic at runtime, what is still engineering?
probe:
  id: ClaudeCode
  q: Which layers of a coding-agent harness do you actually use? Which of them is not prompting?
probe:
  id: AgentHook
  q: What is an agent hook, in one sentence? When would you want one that is not a prompt?
probe:
  id: MultiAgent
  q: When do you send work to a subagent instead of keeping it in the main context?
